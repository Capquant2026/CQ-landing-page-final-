version: "3.8"
services:
  landing-page:
    container_name: cq-landing-page-dev
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - WATCHPACK_POLLING=true